  <% if book.errors.any? %>
    <div class="error-explanation">
      <h3><%= pluralize(book.errors.count, "error") %> prohibited from being saved</h3>
      <ul>
        <% book.errors.full_messages.each do |m| %>
          <li><%= m %></li>
        <% end %>
      </ul>
    </div>
  <% end %>